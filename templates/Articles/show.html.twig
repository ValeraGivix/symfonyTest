{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1> {{ article.title }} </h1>

    <p>{{ article.body }}</p>

    <a href="{{ path('list') }}"> return</a>
</div>
{% endblock %}
